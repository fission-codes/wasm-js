{
  "name": "workerd",
  "version": "0.0.0",
  "devDependencies": {
    "vitest": "^0.33.0",
    "wrangler": "3.2.0"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest --run"
  },
  "dependencies": {
    "wasm": "workspace:^"
  }
}

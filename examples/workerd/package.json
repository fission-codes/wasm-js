{
  "name": "workerd",
  "version": "0.0.0",
  "devDependencies": {
    "vitest": "^0.34.2",
    "wrangler": "3.5.1"
  },
  "private": true,
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest --run"
  },
  "dependencies": {
    "wasm": "workspace:^"
  }
}
